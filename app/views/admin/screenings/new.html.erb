<% content_for :title, "Add Show" %>

<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New Show For Screen <%= @screen.screen_name %></h1>

  <%= render "form", screening: @screening %>

  <%= link_to "Cancel", admin_screen_screenings_path(@screen), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
