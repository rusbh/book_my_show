<div class="w-full">
  <div class="flex justify-between mb-10 gap-x-5" data-turbo="false">
   <a href="/shows/t20-world-cup-2024-india-vs-pakistan"><img src="/indvspak-banner.jpeg" alt="" height="500" width="725" class="rounded-xl"></a>
   <a href="/shows/mr-mrs-mahi"><img src="/mr-and-mrs-mahi-banner.jpeg" alt="" height="500" width="725" class="rounded-xl"></a>
  </div>

  <div class="flex justify-between">
    <h1 class="mt-10 mb-5 text-3xl font-semibold">Recommended Shows</h1>
    <p><%= link_to 'See All ›', shows_path, class: "mt-10 ml-2 py-3 px-5 inline-block font- text-xl" %></p>
  </div>

<% if @recommended_shows.present? %>
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-5 gap-4">
    <% @recommended_shows.each do |show| %>
      <a href="<%= show_path(show) %>">
        <%= image_tag url_for(show.poster.variant(:display)), class: "rounded-lg hover:scale-105 cursor-pointer transition" if show.poster.attached? %>
        <h1 class="text-2xl my-4 font-semibold"><%= show.name %></h1>
      </a>
    <% end %>
  </div>
<% else %>
  <h1 class="text-xl">No shows are currently running in theaters</h1>
<% end %>

<h1 class="font-bold text-3xl my-5 underline">Action packed</h1>

<% if @action_shows.present? %>
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-5 gap-4">
    <% @action_shows.each do |show| %>
      <a href="<%= show_path(show) %>">
        <%= image_tag url_for(show.poster.variant(:display)), class: "rounded-lg hover:scale-105 cursor-pointer transition" if show.poster.attached? %>
        <h1 class="text-2xl my-4 font-semibold"><%= show.name %></h1>
      </a>
    <% end %>
  </div>
<% else %>
  <h1 class="text-xl">No action shows currently running in theaters</h1>
<% end %>
  
<h1 class="font-bold text-3xl my-5 underline">In Gujarati</h1>

<% if @gujarati_shows.present? %>
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-5 gap-4">
    <% @gujarati_shows.each do |show| %>
      <a href="<%= show_path(show) %>">
        <%= image_tag url_for(show.poster.variant(:display)), class: "rounded-lg hover:scale-105 cursor-pointer transition" if show.poster.attached? %>
        <h1 class="text-2xl my-4 font-semibold"><%= show.name %></h1>
      </a>
    <% end %>
  </div>
<% else %>
  <h1 class="text-xl">No Gujarati shows currently running in theaters</h1>
<% end %>

<h1 class="font-bold text-3xl my-5 underline">In sports/plays/events</h1>

<% if @except_movies.present? %>
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-5 gap-4">
    <% @except_movies.each do |show| %>
      <a href="<%= show_path(show) %>">
        <%= image_tag url_for(show.poster.variant(:display)), class: "rounded-lg hover:scale-105 cursor-pointer transition" if show.poster.attached? %>
        <h1 class="text-2xl my-4 font-semibold"><%= show.name %></h1>
      </a>
    <% end %>
  </div>
<% else %>
  <h1 class="text-xl">currently there are not these type of shows currently running in theaters</h1>
<% end %>

</div>
